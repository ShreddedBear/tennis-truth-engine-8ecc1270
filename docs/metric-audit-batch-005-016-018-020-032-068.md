# Metrics 005, 016, 018, 020, 032, 068 (batch) — Sequential Audit Record

Status: all PARTIAL. No code changes this pass — every one of these
already has a real, live, tested engine (mostly Task 18A/18B), just no
narrative `docs/metric-audit-0XX.md` until now. This closes out the last
of the "connected but undocumented" set identified during the
reconciliation audit, and with it, every one of the 59 true
denominator codes has now been examined at least once this session (40
PARTIAL, 10 AI-dependent, 2 UNKNOWN_REQUIRES_REVIEW, 1 TRULY_UNAVAILABLE,
plus these 6 — see `docs/evidence-work-blockers.md` item 0 for the
running tally).

| Code | Real name | Engine | Bullets covered / total | Notes |
|---|---|---|---|---|
| 005 | Recent Form | `predixsport-recent.server.ts` via `hybrid-audit-research.server.ts`'s `SUMMARY_KEYS["005"]`, plus `historical-results-recovery.ts`'s Task 18A branch | 6-7 / 8 | Last 5/10 Match Performance, Trend Direction, Recent-Performance Acceleration, Average Games/Sets Conceded in Wins, and Straight-Set Control Rate are all directly computed. Quality of Last 3-5 Performances (opponent-strength-weighted) is not. Current Hard-Court Swing is only loosely approximated by `current_surface_recent_win_pct` (a general current-surface window, not specifically a "current run of hard-court tournaments"). Two independent engines both target this code; not fully reconciled against each other in this pass, but both agree on the strong core coverage. |
| 016 | Point-by-Point & Score-State Metrics | `pbp-score-state-recovery.ts` (Task 18B) | 6-7 / 16 | Score-State Performance at 0-30/15-30/30-30/Deuce/Advantage/Break Point (6 of 8 named score-state bullets; Set Point and Match Point states are not separately tagged) plus a longest-point-win-streak figure for Point-by-Point Sequencing. Serve-Direction Patterns, Return Positioning/Depth, Rally-Length Performance, Winner/Unforced-Error Differential, and Forced-Error Generation all correctly stay uncovered (shot-level data this system does not have). Already well self-documented in the engine's own code comments. |
| 018 | Momentum & Closing Metrics | `pbp-score-state-recovery.ts` (Task 18B) | 2 / 3 (with a caveat) | Closing Ability (serving-for-match/set, one-break-up) is a strong, direct match via `closeout_rate_pct`. Performance Following Momentum Events is only partially satisfied -- `breakback_rate_pct` covers the "being broken, then holding the return game" case, but the bullet's own definition also includes "after ... winning/losing a tiebreak," which this engine does not track. Opponent Comeback Susceptibility (the *opponent's* tendency to allow comebacks) is not computed at all. **Flagged, not changed:** this code is currently the only Task 18B code marked `RECONSTRUCTED` rather than `PARTIAL`; given the tiebreak-momentum gap and the fully-uncovered third bullet, the same house-rule reasoning already applied to codes 001/014/015 (RECONSTRUCTED requires every named component) would suggest PARTIAL is more accurate. Not changed this pass -- no blast-radius check was done and this session's remaining scope was to close out documentation coverage, not open a new treatment-correction thread this late in the batch. Recommended as the next concrete follow-up. |
| 020 | Level/Tour Transition | `deterministic-results-schedule-metrics.server.ts` / `hybrid-audit-research.server.ts`'s `SUMMARY_KEYS["020"]` (`same_level_matches`, `same_level_win_pct`) | Questionable / thin | Real 020 needs "Tour-Level Transition Performance" -- results specifically around the *moment* of moving between tour levels (ITF→Challenger→ATP/WTA, qualifying→main draw) -- and "Performance vs Opponent Elo Differential" and "Post-Strong/Weak-Tournament Performance" (regression-to-mean after unusual runs). What's actually computed (`same_level_matches`/`same_level_win_pct`) is a player's aggregate win rate *at* a given level, not their performance specifically *at the transition point* between levels -- a plausible instance of the same "wrong-granularity credit" pattern already found for 021/060/071 (see item 4). Not changed this pass for the same reason as those: needs a considered look, not a same-pass unilateral change. |
| 032 | Point-to-Game Conversion Efficiency | `pbp-score-state-recovery.ts` (Task 18B) | 1 / 10 | Already explicitly self-documented in the engine's own code: "only 'break opportunities per successful break' of this composite metric's 10 named sub-components... is covered by deterministic replay." Points-to-games/sets expectation modeling, deuce-game win rate, and 0-30/15-30/30-0/40-15 game-state splits are correctly left uncovered. |
| 068 | Streaks / Milestones | `historical-results-recovery.ts` (Task 18A, `code==="068"` branch) | 3 / 4 | Current Win/Loss Streak Length, Longest Win Streak This Season, and Tournament Debut Status (when a tournament is supplied) are all directly computed from chronological results. Protected-Ranking Status is correctly left uncovered -- no injury-protected-ranking flag exists in any ingested source, and the engine's own comment says so explicitly. |

## Denominator effect

None. All six were already `LEGITIMATE_PLAYER_METRIC` in
`metric-classification.ts` and already counted in the true 59-code
denominator; this pass adds documentation and one flagged-but-unfixed
treatment concern (018), not a classification change.

## Follow-ups logged, not actioned this pass

- **018's RECONSTRUCTED treatment** may be over-claimed relative to its
  real 3-bullet definition (see table above) -- the same class of issue
  already fixed for 001/014/015. Recommended next concrete fix.
- **020's granularity mismatch** (aggregate same-level performance vs.
  transition-specific performance) needs the same considered look already
  pending for the item-4 family (021/060/071).

## Regression protection

No new tests added -- none of these three engines' actual code changed
this pass, only documentation. The follow-ups above, if actioned, should
each get the same blast-radius-checked treatment-correction pattern
already used for 001/014/015/043/044.

Certification: documentation-complete for all six; no evidence inflation
found or introduced. Two genuine follow-up items logged for a future pass.

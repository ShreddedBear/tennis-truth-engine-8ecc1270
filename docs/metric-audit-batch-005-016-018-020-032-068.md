# Metrics 005, 016, 018, 020, 032, 068 (batch) — Sequential Audit Record

Status: 005/016/032/068 PARTIAL, documentation only. 018 FIXED
(RECONSTRUCTED → PARTIAL). 020 FIXED (false credit removed; now correctly
SOURCE REQUIRED). This closes out the last of the "connected but
undocumented" set identified during the reconciliation audit, and with
it, every one of the 59 true denominator codes has now been examined at
least once this session — see `docs/evidence-work-blockers.md` item 0 for
the running tally.

**Update (same session, follow-up pass):** both flagged concerns below
were investigated further and fixed, not left open.

| Code | Real name | Engine | Bullets covered / total | Notes |
|---|---|---|---|---|
| 005 | Recent Form | `predixsport-recent.server.ts` via `hybrid-audit-research.server.ts`'s `SUMMARY_KEYS["005"]`, plus `historical-results-recovery.ts`'s Task 18A branch | 6-7 / 8 | Last 5/10 Match Performance, Trend Direction, Recent-Performance Acceleration, Average Games/Sets Conceded in Wins, and Straight-Set Control Rate are all directly computed. Quality of Last 3-5 Performances (opponent-strength-weighted) is not. Current Hard-Court Swing is only loosely approximated by `current_surface_recent_win_pct` (a general current-surface window, not specifically a "current run of hard-court tournaments"). Two independent engines both target this code; not fully reconciled against each other in this pass, but both agree on the strong core coverage. |
| 016 | Point-by-Point & Score-State Metrics | `pbp-score-state-recovery.ts` (Task 18B) | 6-7 / 16 | Score-State Performance at 0-30/15-30/30-30/Deuce/Advantage/Break Point (6 of 8 named score-state bullets; Set Point and Match Point states are not separately tagged) plus a longest-point-win-streak figure for Point-by-Point Sequencing. Serve-Direction Patterns, Return Positioning/Depth, Rally-Length Performance, Winner/Unforced-Error Differential, and Forced-Error Generation all correctly stay uncovered (shot-level data this system does not have). Already well self-documented in the engine's own code comments. |
| 018 | Momentum & Closing Metrics | `pbp-score-state-recovery.ts` (Task 18B) | 2 / 3 (with a caveat) | Closing Ability is a strong, direct match via `closeout_rate_pct`. Performance Following Momentum Events is only partially satisfied (`breakback_rate_pct` covers the being-broken half, not the tiebreak-momentum half of that bullet's own definition). Opponent Comeback Susceptibility is not computed at all. **FIXED:** downgraded `RECONSTRUCTED` → `PARTIAL` (was the only Task 18B code claiming the full bar despite a real gap, same house-rule issue already fixed for 001/014/015). Blast radius checked: `pbp-score-state-recovery.test.ts` was the only test asserting this specific treatment value; updated. Full suite passes. |
| 020 | Level/Tour Transition | *(previously wired to `deterministic-results-schedule-metrics.server.ts` / `hybrid-audit-research.server.ts`'s `SUMMARY_KEYS["020"]` / `historical-results-recovery.ts`'s Task 18A branch / `wta-official-match-evidence.server.ts` / `completion-sweep-research.server.ts` — all now removed)* | 0 / 3 (all prior "coverage" was a wrong-grain proxy) | **FIXED, more serious than initially flagged.** `historical-results-recovery.ts`'s `code==="020"` branch computed a 90-day opponent-quality-banded win rate that doesn't correspond to any of 020's 3 real bullets at all (it's much closer to a "Recent Quality" concept that doesn't exist anywhere in the real 81-section catalog) -- and unlike every other code in that file's `TASK18A_HISTORICAL_RESULTS_CODES` set, it had never received its own individual justification comment, a strong signal it was simply carried over unreviewed. It was also being swept into that file's generic "asserts RECONSTRUCTED" test loop -- the highest treatment bar, for content unrelated to the real definition. Separately, `same_level_matches`/`same_level_win_pct` (wired in 3 more places) measures aggregate performance *at* a level, not performance specifically *at the transition* between levels. **Removed all of it** rather than leave any of it as false credit: `historical-results-recovery.ts` (code branch + `TASK18A_HISTORICAL_RESULTS_CODES`, 10→9 codes), `hybrid-audit-research.server.ts`'s `SUMMARY_KEYS`, `completion-sweep-research.server.ts`'s `HISTORICAL_KEYS`, and `wta-official-match-evidence.server.ts`'s `SUPPORTED` set + case branch. 020 now has **no deterministic evidence at all** -- reclassified PARTIAL → **SOURCE REQUIRED**. Blast radius checked across all 4 files; only `historical-results-recovery.test.ts`'s exact-code-list assertion needed updating. Full suite passes. |
| 032 | Point-to-Game Conversion Efficiency | `pbp-score-state-recovery.ts` (Task 18B) | 1 / 10 | Already explicitly self-documented in the engine's own code: "only 'break opportunities per successful break' of this composite metric's 10 named sub-components... is covered by deterministic replay." Points-to-games/sets expectation modeling, deuce-game win rate, and 0-30/15-30/30-0/40-15 game-state splits are correctly left uncovered. |
| 068 | Streaks / Milestones | `historical-results-recovery.ts` (Task 18A, `code==="068"` branch) | 3 / 4 | Current Win/Loss Streak Length, Longest Win Streak This Season, and Tournament Debut Status (when a tournament is supplied) are all directly computed from chronological results. Protected-Ranking Status is correctly left uncovered -- no injury-protected-ranking flag exists in any ingested source, and the engine's own comment says so explicitly. |

## Denominator effect

None of the six changed which codes count in the denominator -- all
remain `LEGITIMATE_PLAYER_METRIC`. 020's classification within the
denominator changed from PARTIAL to SOURCE REQUIRED (evidence removed,
not the code excluded).

## Fixes applied this follow-up pass

- **018**: `RECONSTRUCTED` → `PARTIAL` in `pbp-score-state-recovery.ts`,
  with an expanded transformation-text explanation. Test updated.
- **020**: removed false credit from 4 files (see table above). No
  retarget attempted -- the content didn't cleanly match any other real
  code either, and guessing a new home would repeat the same mistake.
  020 is now honestly SOURCE REQUIRED. Tests updated.
- The item-4 pattern (021/060/071, wrong-granularity/wrong-family
  evidence on a shared engine) is now confirmed for a **fourth** distinct
  code (020, via a completely different engine) -- raises confidence this
  is a systemic pattern worth a dedicated sweep, not four unrelated
  coincidences. Logged in `docs/evidence-work-blockers.md` item 4.

## Regression protection

`pbp-score-state-recovery.test.ts` and `historical-results-recovery.test.ts`
updated in place (existing files) for the 018 and 020 fixes respectively.
Full 618-test suite passes after both changes; no new tsc errors.

Certification: documentation-complete for 005/016/032/068; FIXED for 018
and 020. No evidence inflation found or introduced; two real
over-claims corrected.

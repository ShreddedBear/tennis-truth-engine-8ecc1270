Correct. Do not run the 50% replay yet.

The canonical repository remains:

ShreddedBear/tennis-truth-engine-8ecc1270

Do NOT switch the project back to Tennis-Stats-Engine, do NOT create another repository merge, and do NOT commit or push anything during this task.

We need to establish whether Candidate B’s behavior, rather than its original Git SHA, was successfully incorporated into the consolidated Prediction Engine.

TASK — SOURCE-LEVEL CANDIDATE B PROVENANCE

Use the actual canonical repository source at the current consolidated branch/commit.

1. Locate the canonical Prediction Engine

Identify the exact files implementing:

* Surface Elo
* sample-size handling
* low-surface-sample probability adjustment
* final probability generation
* calibration/ensemble logic relevant to this behavior

2. Search for the legacy behavior

Search the entire canonical repository for:

LOW_SURFACE_SAMPLE_DISCOUNT

Also search for equivalent logic/constants if the identifier was renamed or removed.

Determine:

* Does the constant still exist?
* Does an equivalent probability discount still exist?
* Where is the adjustment applied?
* Is Surface Elo’s internal sample-size handling still present?
* Is sample size also used in ensemble weighting/Data Quality?

3. Identify canonical provenance

Use Git history/blame in the canonical repository to determine:

* the commit that introduced the current low-surface-sample behavior;
* the commit that removed/retired the dedicated probability discount, if applicable;
* whether this corresponds functionally to Candidate B;
* whether a later commit modified that behavior.

Do NOT require the original legacy SHA to be present.

4. Compare against the actual Candidate B implementation

The original Candidate B SHA is:

d5f0f198ff05cf2643cb39269a593762198ea5aa

If the legacy commit/object is accessible through the authorized GitHub connection or local Git object store, inspect its actual diff.

Compare behavior, not just commit identity.

Determine whether canonical code is:

A. Baseline behavior
B. Candidate B behavior
C. Later equivalent/improved behavior
D. Neither

If Candidate B’s actual diff cannot be accessed, say so explicitly. Do not reconstruct it from prior reports alone.

5. Determine replay feasibility

A valid 50% replay requires both:

* a reproducible baseline implementation;
* a reproducible Candidate B implementation;

using the same historical data and cutoff rules.

Determine whether both can be reconstructed inside the canonical repository without altering production behavior.

Do not run the replay yet.

Do not modify the database.

Do not create commits.

Do not push.

RETURN EXACTLY

CANONICAL PREDICTION ENGINE

* location:
* current commit:
* Surface Elo implementation:
* probability generation implementation:

LOW-SURFACE BEHAVIOR

* LOW_SURFACE_SAMPLE_DISCOUNT present: YES/NO
* equivalent dedicated probability discount present: YES/NO
* Surface Elo sample-size handling present: YES/NO
* sample-size ensemble/Data Quality handling present: YES/NO

GIT PROVENANCE

* canonical commit removing/retiring dedicated discount:
* canonical commit introducing current behavior:
* Candidate B behavior matches canonical implementation: YES/NO/UNKNOWN
* later modifications after Candidate B: YES/NO
* evidence:

REPLAY

* baseline reproducible in canonical repo: YES/NO
* Candidate B reproducible in canonical repo: YES/NO
* same data/cutoff can be used: YES/NO
* 50% replay ready: YES/NO

FINAL CLASSIFICATION

Choose exactly one:

BASELINE

CANDIDATE B

LATER EQUIVALENT/IMPROVED IMPLEMENTATION

NEITHER

UNKNOWN — INSUFFICIENT PROVENANCE

If replay is not ready, identify the exact missing artifact or code path.

Again: no code changes, no database changes, no commits, no pushes, and no replay in this task.
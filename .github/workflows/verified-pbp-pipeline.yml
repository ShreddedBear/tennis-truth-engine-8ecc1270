name: Verified ATP WTA PBP Pipeline v4 — CANCELLED

# CANCELLED by user on 2026-08-23.
# Historical PBP audit outputs are retained for provenance only.
# No push, schedule, or manual workflow trigger remains enabled.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cancelled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'PBP v4 pipeline cancelled by user.'

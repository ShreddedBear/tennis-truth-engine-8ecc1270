# The database. This is the only configuration the data layer needs.
# Any PostgreSQL 14+ server works: a Supabase project's connection string, Neon, RDS,
# Railway, or a local container.
DATABASE_URL=postgresql://user:password@host:5432/database

# Optional. Maximum connections per application instance (default 10).
# DATABASE_POOL_MAX=10

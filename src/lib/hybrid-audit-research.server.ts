// Hybrid audit researcher. Local public historical evidence is always attempted first; live AI/web research supplements it when available.
import type { IdentityFinding, Researcher, MetricFinding } from "./audit-pipeline";
import type { SourcedStat } from "./reconstruction/engine";
import { aiResearcher } from "./audit-research.server";
import { getPredixDatasetEvidence, predixDatasetDossier, statsFromPredixDatasetDossier } from "./predixsport-dataset.server";
import { getRecentReconstruction } from "./predixsport-recent.server";
import { getDerivedHistoricalStats } from "./predixsport-derived.server";
import { getStrengthTrajectoryStats } from "./predixsport-strength.server";
import { getRankingPerformanceStats } from "./ranking-performance.server";
import { getH2HStats } from "./predixsport-h2h.server";
import { getHistoricalServeReturnStats } from "./datahub-atp-serve-return.server";
import { getCommonOpponentEvidence } from "./predixsport-common.server";
import { resolveLocalMatchContext } from "./local-match-context.server";
function isProviderFailure(error:unknown){const m=error instanceof Error?error.message.toLowerCase():String(error).toLowerCase();return /402|credit|quota|429|rate limit|timeout|provider|api key|auth|fetch|not configured/.test(m);}
function localIdentity(input:{p1:string;p2:string;hints:Record<string,string|null>}):IdentityFinding{const ctx=resolveLocalMatchContext(input.p1,input.p2,input.hints),context=Object.entries(ctx.fields).filter(([,v])=>v).map(([k,v])=>`${k} ${v}`).join(" · "),a=getPredixDatasetEvidence(input.p1,context),b=getPredixDatasetEvidence(input.p2,context);return{player1_canonical:a?.canonicalPlayer??(a?input.p1:null),player2_canonical:b?.canonicalPlayer??(b?input.p2:null),player1_status:a?"VERIFIED":"UNVERIFIED",player2_status:b?"VERIFIED":"UNVERIFIED",tournament:ctx.fields.tournament??null,event_level:ctx.fields.event_level??null,round:ctx.fields.round??null,scheduled_date:ctx.fields.scheduled_date??null,surface:ctx.fields.surface??null,indoor:null,best_of:ctx.fields.best_of?Number(ctx.fields.best_of):null,surface_status:ctx.fields.surface?"VERIFIED":"UNVERIFIED",unresolved_reason:ctx.unresolvedReason,sources:ctx.sources.map(name=>({source_name:name,url:ctx.sourceUrl,retrieved_at:new Date().toISOString()})),conflicts:[]};}
function mergeIdentity(a:IdentityFinding,b:IdentityFinding):IdentityFinding{const c=<T>(x:T|null|undefined,y:T|null|undefined)=>y??x??null;return{player1_canonical:c(a.player1_canonical,b.player1_canonical),player2_canonical:c(a.player2_canonical,b.player2_canonical),player1_status:b.player1_status==="VERIFIED"?"VERIFIED":a.player1_status,player2_status:b.player2_status==="VERIFIED"?"VERIFIED":a.player2_status,tournament:c(a.tournament,b.tournament),event_level:c(a.event_level,b.event_level),round:c(a.round,b.round),scheduled_date:c(a.scheduled_date,b.scheduled_date),surface:c(a.surface,b.surface),indoor:c(a.indoor,b.indoor),best_of:c(a.best_of,b.best_of),surface_status:b.surface_status==="VERIFIED"?"VERIFIED":a.surface_status,unresolved_reason:b.unresolved_reason??a.unresolved_reason,sources:[...(a.sources??[]),...(b.sources??[])],conflicts:[...(a.conflicts??[]),...(b.conflicts??[])]};}
function familyCode(code:string){const m=String(code).match(/(\d{1,3})$/);return m?m[1].padStart(3,"0"):String(code).padStart(3,"0");}
function summarize(map:Map<string,SourcedStat>,keys:string[]){const p=keys.map(k=>map.get(k)).filter((x):x is SourcedStat=>!!x);return p.length?p.map(s=>`${s.key}=${Number(s.value).toFixed(2)}`).join("; "):null;}
function summaryFor(map:Map<string,SourcedStat>,code:string){const f=familyCode(code),get=(k:string)=>map.get(k)?.value;let value:string|null=null;if(f==="001")value=summarize(map,["surface_elo","current_surface_elo","peak_surface_elo","observed_peak_surface_elo","surface_win_pct","surface_matches","surface_elo_trend","surface_elo_change_last10","peak_vs_current_elo_gap","surface_elo_below_peak","surface_win_pct_52w","surface_matches_52w","current_overall_elo","career_observed_peak_elo","elo_below_peak","elo_change_last5","elo_change_last10","elo_change_last20","strength_vs_peak_pct"]);if(f==="002")value=summarize(map,["service_points_won_pct","first_serve_in_pct","first_serve_points_won_pct","second_serve_points_won_pct","ace_rate_pct","double_fault_rate_pct","break_points_saved_pct","hold_pct","service_games_held"]);if(f==="003")value=summarize(map,["return_points_won_pct","first_serve_return_points_won_pct","second_serve_return_points_won_pct","break_point_conversion_pct","break_pct","return_games_played","break_points_created_per_return_game"]);if(f==="004")value=summarize(map,["service_points_won_pct","return_points_won_pct","total_points_won_pct","hold_pct","break_pct"]);if(f==="005")value=summarize(map,["last5_win_pct","last10_win_pct","last5_set_win_pct","last10_set_win_pct","current_surface_recent_win_pct","win_pct_60d","win_pct_90d","recent_form_trend","recent_straight_set_control_pct","recent_performance_acceleration","avg_sets_conceded_in_recent_wins","set_margin_mean","overall_recent20_win_pct","elo_change_last5","elo_change_last10","elo_change_last20","elo_change_per_match_last20","recent_form_strength_signal"]);if(f==="006")value=summarize(map,["recent_opponent_avg_elo","best_recent_win_opponent_elo","bad_loss_rate_pct","comparable_strength_win_pct","current_overall_elo","performance_vs_comparable_strength_pct"]);if(f==="008"){const wp=get("set_win_pct"),n=get("sets_played"),w=get("sets_won");if(wp!==undefined)value=`Set win ${Number(wp).toFixed(1)}%; sets ${w??"—"}/${n??"—"}`;}if(f==="009")value=summarize(map,["deciding_set_win_pct","deciding_sets_played","deciding_sets_won","break_points_saved_pct","break_point_conversion_pct","close_match_win_pct"]);if(f==="010")value=summarize(map,["straight_set_win_pct","straight_set_wins","matches_won","straight_set_rate_comparable_pct"]);if(f==="011")value=summarize(map,["performance_variance","floor_ceiling_elo_range","deciding_match_reliance_pct","close_match_win_pct","upset_resistance_pct","recent_elo_delta_mean","recent_elo_delta_variance","recent_elo_best_delta","recent_elo_worst_delta","career_observed_peak_elo","career_observed_low_elo","elo_below_peak","elo_change_last20","surface_elo_below_peak","favorite_fragility_strength_gap"]);if(f==="012")value=summarize(map,["matches_last_7_days","matches_last_14_days","matches_last_28_days","sets_last_14_days","three_setters_last_14_days","rest_days","qualifying_matches_last_14_days"]);if(f==="013")value=summarize(map,["strength_vs_peak_pct","performance_vs_comparable_strength_pct","recent_form_strength_signal","surface_performance_strength_signal","favorite_fragility_strength_gap","current_overall_elo","career_observed_peak_elo","elo_change_last20","surface_elo_change_last10"]);if(f==="014"){const w=get("wins"),l=get("losses"),wp=get("win_pct");if(w!==undefined||l!==undefined)value=`Historical record ${w??"—"}-${l??"—"}; win ${wp!==undefined?Number(wp).toFixed(1)+"%":"—"}`;}if(!value)return null;const first=[...map.values()][0];return{value,sample:first?.sample??null,sources:first?.sources??[]};}
function metricLooksH2H(m:{name:string;body:string|null}){return /\bh2h\b|head\s*[- ]?to\s*[- ]?head|direct meetings?|prior meetings?/i.test(`${m.name} ${m.body??""}`);}
function h2hSummary(map:Map<string,SourcedStat>){const value=summarize(map,["h2h_matches","h2h_wins","h2h_win_pct","h2h_surface_matches","h2h_surface_wins","h2h_surface_win_pct","h2h_recent3_win_pct"]),first=[...map.values()].find(s=>s.key.startsWith("h2h_"));return value?{value,sample:first?.sample??null,sources:first?.sources??[]}:null;}
function allStats(p:string,o:string,c:string){const d=getPredixDatasetEvidence(p,c);return[...(d?.stats??[]),...getRecentReconstruction(p,c),...getDerivedHistoricalStats(p,c),...getStrengthTrajectoryStats(p,c),...getRankingPerformanceStats(p,c),...getH2HStats(p,o,c),...getHistoricalServeReturnStats(p,c)];}
function localMetricRows(p1:string,p2:string,context:string,requested:Array<{code:string;name:string;body:string|null}>):MetricFinding[]{const amap=new Map(allStats(p1,p2,context).map(s=>[s.key,s])),bmap=new Map(allStats(p2,p1,context).map(s=>[s.key,s])),common=getCommonOpponentEvidence(p1,p2,context);return requested.map(m=>{let xs=metricLooksH2H(m)?h2hSummary(amap):summaryFor(amap,m.code),ys=metricLooksH2H(m)?h2hSummary(bmap):summaryFor(bmap,m.code);if(familyCode(m.code)==="007"&&common){const src=[common.source];xs={value:`${common.commonCount} common opponents; ${common.p1Wins}-${common.p1Losses}; win ${common.p1WinPct?.toFixed(1)??"—"}%`,sample:common.commonCount,sources:src};ys={value:`${common.commonCount} common opponents; ${common.p2Wins}-${common.p2Losses}; win ${common.p2WinPct?.toFixed(1)??"—"}%`,sample:common.commonCount,sources:src};}const sources=[...(xs?.sources??[]),...(ys?.sources??[])].filter((s,i,a)=>a.findIndex(z=>z.source_name===s.source_name&&z.url===s.url)===i),historicalSR=sources.some(s=>s.source_name.includes("DataHub ATP"));return{metric_code:m.code,p1_value:xs?.value??null,p2_value:ys?.value??null,p1_treatment:xs?"RECONSTRUCTED":"UNAVAILABLE",p2_treatment:ys?"RECONSTRUCTED":"UNAVAILABLE",differential:null,evidence_family:xs||ys?(metricLooksH2H(m)?"PUBLIC_HISTORICAL_H2H":historicalSR?`HISTORICAL_SERVE_RETURN_FAMILY_${familyCode(m.code)}`:`PUBLIC_HISTORICAL_DATA_FAMILY_${familyCode(m.code)}`):null,reliability:xs||ys?(historicalSR?70:85):null,sample:String(Math.max(xs?.sample??0,ys?.sample??0))||null,unavailable_reason:!xs&&!ys?"Synced public historical data does not support this metric family":null,sources} as MetricFinding;});}
function mergeMetrics(live:MetricFinding[],local:MetricFinding[]):MetricFinding[]{const by=new Map(live.map(m=>[String(m.metric_code),m]));return local.map(l=>{const m=by.get(String(l.metric_code));if(!m)return l;const p1=m.p1_treatment!=="UNAVAILABLE"&&m.p1_treatment!=="EXCLUDED"&&m.p1_value!==null,p2=m.p2_treatment!=="UNAVAILABLE"&&m.p2_treatment!=="EXCLUDED"&&m.p2_value!==null;return{...m,p1_value:p1?m.p1_value:l.p1_value,p1_treatment:p1?m.p1_treatment:(l.p1_value!==null?l.p1_treatment:m.p1_treatment),p2_value:p2?m.p2_value:l.p2_value,p2_treatment:p2?m.p2_treatment:(l.p2_value!==null?l.p2_treatment:m.p2_treatment),evidence_family:m.evidence_family??l.evidence_family,reliability:m.reliability??l.reliability,sample:m.sample??l.sample,sources:[...(m.sources??[]),...(l.sources??[])].filter((s,i,a)=>a.findIndex(z=>z.source_name===s.source_name&&z.url===s.url)===i),unavailable_reason:(p1||p2||l.p1_value!==null||l.p2_value!==null)?null:(m.unavailable_reason??l.unavailable_reason)};});}
export const hybridResearcher:Researcher={async identity(input){const local=localIdentity(input);try{return mergeIdentity(local,await aiResearcher.identity(input));}catch(e){if(!isProviderFailure(e))throw e;return local;}},async dossier({player,opponent,context}){const local=predixDatasetDossier(player,context);try{return[local,await aiResearcher.dossier?.({player,opponent,context})??""].filter(Boolean).join("\n");}catch(e){if(!isProviderFailure(e))throw e;return local;}},async extractStats({player,opponent,dossier,context}){const local=[...statsFromPredixDatasetDossier(dossier,player),...getRecentReconstruction(player,context),...getDerivedHistoricalStats(player,context),...getStrengthTrajectoryStats(player,context),...getRankingPerformanceStats(player,context),...getH2HStats(player,opponent,context),...getHistoricalServeReturnStats(player,context)];try{const live=await aiResearcher.extractStats?.({player,dossier,context})??[],seen=new Set(live.map(s=>`${s.key}|${s.surface??""}|${s.window??""}`));return[...live,...local.filter(s=>!seen.has(`${s.key}|${s.surface??""}|${s.window??""}`))];}catch(e){if(!isProviderFailure(e))throw e;return local;}},async metrics(input){const local=localMetricRows(input.p1,input.p2,input.context,input.metrics);try{return mergeMetrics(await aiResearcher.metrics(input),local);}catch(e){if(!isProviderFailure(e))throw e;return local;}},rules:i=>aiResearcher.rules(i),underdog:i=>aiResearcher.underdog(i),conclusion:i=>aiResearcher.conclusion(i),stress:i=>aiResearcher.stress(i)};

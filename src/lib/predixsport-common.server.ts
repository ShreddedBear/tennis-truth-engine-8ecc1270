import { readFileSync } from "node:fs";
import { join } from "node:path";
const SOURCE_URL="https://www.kaggle.com/datasets/predixsport/sports-elo-ratings",SOURCE_NAME="PredixSport public tennis ratings (CC BY 4.0)";type Row=Record<string,string>;let cache:Row[]|null=null;
function norm(v:string){return v.normalize("NFKD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim();}function tokens(v:string){return norm(v).split(" ").filter(Boolean);}function parseCsv(text:string){const rows:string[][]=[];let row:string[]=[],cell="",q=false;for(let i=0;i<text.length;i++){const ch=text[i];if(ch==='"'){if(q&&text[i+1]==='"'){cell+='"';i++;}else q=!q;}else if(ch===","&&!q){row.push(cell);cell="";}else if((ch==="\n"||ch==="\r")&&!q){if(ch==="\r"&&text[i+1]==="\n")i++;row.push(cell);cell="";if(row.some(Boolean))rows.push(row);row=[];}else cell+=ch;}if(cell||row.length){row.push(cell);rows.push(row);}if(!rows.length)return[];const h=rows[0].map(x=>x.trim());return rows.slice(1).map(c=>Object.fromEntries(h.map((k,i)=>[k,(c[i]??"").trim()])));}
function load(){if(cache)return cache;try{return cache=parseCsv(readFileSync(join(process.cwd(),"data/public/predixsport/atp/atp_elo_matches.csv"),"utf8"));}catch{return cache=[];}}function resolve(rows:Row[],name:string){const n=norm(name),names=[...new Set(rows.map(r=>r.player).filter(Boolean))],exact=names.filter(x=>norm(x)===n);if(exact.length===1)return exact[0];const t=tokens(name),last=t[t.length-1];if(!last)return null;const c=names.filter(x=>{const xt=tokens(x);if(xt[xt.length-1]!==last)return false;const s=new Set(xt);return t.length===1||t.every(v=>s.has(v));});return c.length===1?c[0]:null;}function cutoff(c:string){return c.match(/(?:date\s+)?(20\d{2}-\d{2}-\d{2})/i)?.[1]??null;}function surface(c:string){return c.match(/surface\s+(hard|clay|grass|carpet)/i)?.[1]?.toLowerCase()??null;}
export interface CommonOpponentEvidence{p1:string;p2:string;commonCount:number;p1Wins:number;p1Losses:number;p2Wins:number;p2Losses:number;p1WinPct:number|null;p2WinPct:number|null;source:{source_name:string;url:string;retrieved_at:string};}
export function getCommonOpponentEvidence(p1:string,p2:string,context:string):CommonOpponentEvidence|null{const rows=load(),a=resolve(rows,p1),b=resolve(rows,p2);if(!a||!b)return null;const cut=cutoff(context),surf=surface(context),usable=(r:Row)=>!cut||(!!r.date&&r.date<cut),same=(r:Row)=>!surf||(!!r.surface&&r.surface.toLowerCase()===surf),filtered=rows.filter(r=>usable(r)&&same(r)),ar=filtered.filter(r=>r.player===a),br=filtered.filter(r=>r.player===b),ao=new Set(ar.map(r=>norm(r.opponent??"")).filter(Boolean)),bo=new Set(br.map(r=>norm(r.opponent??"")).filter(Boolean)),common=[...ao].filter(x=>bo.has(x));if(!common.length)return null;const cs=new Set(common),aw=ar.filter(r=>cs.has(norm(r.opponent??""))&&r.won==="1").length,al=ar.filter(r=>cs.has(norm(r.opponent??""))&&r.won==="0").length,bw=br.filter(r=>cs.has(norm(r.opponent??""))&&r.won==="1").length,bl=br.filter(r=>cs.has(norm(r.opponent??""))&&r.won==="0").length;return{p1:a,p2:b,commonCount:common.length,p1Wins:aw,p1Losses:al,p2Wins:bw,p2Losses:bl,p1WinPct:aw+al?100*aw/(aw+al):null,p2WinPct:bw+bl?100*bw/(bw+bl):null,source:{source_name:SOURCE_NAME,url:SOURCE_URL,retrieved_at:new Date().toISOString()}};}

import { existsSync, readFileSync, readdirSync } from "node:fs";
import { join } from "node:path";
import type { SourcedStat } from "./reconstruction/engine";
import { dedupeHistoricalMatches, enrichWithoutDoubleCounting, type HistoricalMatchIdentity } from "./historical-match-dedupe";
import { HISTORICAL_START_DATE, sourceCanContribute, type Tour } from "./historical-source-policy";

type Row=Record<string,string>;
interface MatchRow extends HistoricalMatchIdentity {
  sourceUrl:string; tour:Tour; date:string; tournament:string; location:string; eventLevel:string; court:string; surface:string; round:string; bestOf:string; winner:string; loser:string; winnerRank:string; loserRank:string; score:string; comment:string;
}
const ROOT=join(process.cwd(),"data/public/tennis-data");
const SOURCE_NAME="Tennis-Data historical ATP/WTA results";
let cache:MatchRow[]|null=null;
function norm(v:string|null|undefined){return(v??"").normalize("NFKD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim();}
function parseCsv(text:string):Row[]{const rows:string[][]=[];let r:string[]=[],c="",q=false;for(let i=0;i<text.length;i++){const x=text[i];if(x==='"'){if(q&&text[i+1]==='"'){c+='"';i++;}else q=!q;}else if(x===","&&!q){r.push(c);c="";}else if((x==='\n'||x==='\r')&&!q){if(x==='\r'&&text[i+1]==='\n')i++;r.push(c);c="";if(r.some(Boolean))rows.push(r);r=[];}else c+=x;}if(c||r.length){r.push(c);rows.push(r);}if(!rows.length)return[];const h=rows[0].map(x=>x.trim());return rows.slice(1).map(a=>Object.fromEntries(h.map((k,i)=>[k,(a[i]??"").trim()])));}
function toMatch(r:Row):MatchRow|null{const tour=(r.tour??"").toUpperCase() as Tour;if((tour!=="ATP"&&tour!=="WTA")||!r.date||!r.winner||!r.loser||!sourceCanContribute("tennis-data",tour,r.date))return null;return{source:"tennis-data",sourceMatchId:null,sourceUrl:r.source_url??"https://www.tennis-data.co.uk/alldata.php",tour,date:r.date,tournament:r.tournament??"",location:r.location??"",eventLevel:r.event_level??"",court:r.court??"",surface:r.surface??"",round:r.round??"",bestOf:r.best_of??"",winner:r.winner,loser:r.loser,winnerRank:r.winner_rank??"",loserRank:r.loser_rank??"",score:r.score??"",comment:r.comment??"",player1:r.winner,player2:r.loser};}
function load():MatchRow[]{if(cache)return cache;const all:MatchRow[]=[];try{for(const tour of ["atp","wta"]){const dir=join(ROOT,tour);if(!existsSync(dir))continue;for(const f of readdirSync(dir).filter(x=>/^20\d{2}\.csv$/.test(x)).sort()){for(const r of parseCsv(readFileSync(join(dir,f),"utf8"))){const m=toMatch(r);if(m)all.push(m);}}}}catch{return [];}cache=dedupeHistoricalMatches(all).map(enrichWithoutDoubleCounting);return cache;}
function cut(ctx:string){return ctx.match(/(?:date\s+)?(20\d{2}-\d{2}-\d{2})/i)?.[1]??null;}
function surface(ctx:string){return ctx.match(/surface\s+(hard|clay|grass|carpet)/i)?.[1]?.toLowerCase()??null;}
function tournament(ctx:string){return ctx.match(/tournament\s+([^·]+)/i)?.[1]?.trim()??null;}
function round(ctx:string){return ctx.match(/round\s+([^·]+)/i)?.[1]?.trim()??null;}
function level(ctx:string){return ctx.match(/(?:event_?level|level)\s+([^·]+)/i)?.[1]?.trim()??null;}
function n(v:string){const x=Number(v);return Number.isFinite(x)?x:null;}
function stat(player:string,key:string,value:number,sample:number,url:string,surfaceName:string|null=null):SourcedStat{return{key,player,value,surface:surfaceName,window:"HISTORICAL_2005_PRE_MATCH",tour_level:null,sample,origin:"DIRECT",sources:[{source_name:SOURCE_NAME,url,retrieved_at:null}]};}
function playerMatches(player:string,ctx:string){const pn=norm(player),c=cut(ctx);return load().filter(m=>(!c||m.date<c)&&m.date>=HISTORICAL_START_DATE&&(norm(m.winner)===pn||norm(m.loser)===pn)).sort((a,b)=>a.date.localeCompare(b.date));}
function won(m:MatchRow,p:string){return norm(m.winner)===norm(p);}
function sets(score:string){return score.split(/\s+/).map(s=>s.match(/^(\d+)-(\d+)/)).filter((m):m is RegExpMatchArray=>!!m).map(m=>[Number(m[1]),Number(m[2])] as [number,number]);}
export function getTennisDataHistoricalStats(player:string,context:string):SourcedStat[]{const rows=playerMatches(player,context);if(!rows.length)return[];const s=surface(context),surfaceRows=s?rows.filter(r=>norm(r.surface)===s):rows,chosen=surfaceRows.length?surfaceRows:rows,url=chosen[chosen.length-1]?.sourceUrl??"https://www.tennis-data.co.uk/alldata.php",out:SourcedStat[]=[];const wins=chosen.filter(r=>won(r,player)).length,losses=chosen.length-wins;out.push(stat(player,"wins",wins,chosen.length,url,s),stat(player,"losses",losses,chosen.length,url,s),stat(player,"matches_played",chosen.length,chosen.length,url,s),stat(player,"win_pct",100*wins/chosen.length,chosen.length,url,s));if(s)out.push(stat(player,"surface_matches",chosen.length,chosen.length,url,s),stat(player,"surface_wins",wins,chosen.length,url,s),stat(player,"surface_losses",losses,chosen.length,url,s),stat(player,"surface_win_pct",100*wins/chosen.length,chosen.length,url,s));let setsPlayed=0,setsWon=0,straightWins=0,deciding=0,decidingWins=0;for(const m of chosen){const isW=won(m,player),ss=sets(m.score);if(!ss.length)continue;for(const [a,b] of ss){setsPlayed++;setsWon+=isW?(a>b?1:0):(a<b?1:0);}if(isW&&ss.every(([a,b])=>a>b))straightWins++;if(ss.length>=3){deciding++;if(isW)decidingWins++;}}if(setsPlayed)out.push(stat(player,"sets_played",setsPlayed,chosen.length,url,s),stat(player,"sets_won",setsWon,chosen.length,url,s),stat(player,"set_win_pct",100*setsWon/setsPlayed,chosen.length,url,s));if(wins)out.push(stat(player,"matches_won",wins,chosen.length,url,s),stat(player,"straight_set_wins",straightWins,chosen.length,url,s),stat(player,"straight_set_win_pct",100*straightWins/wins,wins,url,s));if(deciding)out.push(stat(player,"deciding_sets_played",deciding,deciding,url,s),stat(player,"deciding_sets_won",decidingWins,deciding,url,s),stat(player,"deciding_set_win_pct",100*decidingWins/deciding,deciding,url,s));const c=cut(context);if(c){const end=Date.parse(`${c}T00:00:00Z`),start=end-28*86400000,recent=rows.filter(r=>{const t=Date.parse(`${r.date}T00:00:00Z`);return Number.isFinite(t)&&t>=start&&t<end;});out.push(stat(player,"matches_last_28_days",recent.length,recent.length,url,s));const last=rows[rows.length-1];if(last){const days=Math.max(0,Math.floor((end-Date.parse(`${last.date}T00:00:00Z`))/86400000));out.push(stat(player,"days_since_last_match",days,rows.length,url,s));}}const tn=tournament(context),rn=round(context),lv=level(context);if(tn){const z=rows.filter(r=>norm(r.tournament)===norm(tn));if(z.length){const w=z.filter(r=>won(r,player)).length;out.push(stat(player,"same_tournament_matches",z.length,z.length,url,s),stat(player,"same_tournament_win_pct",100*w/z.length,z.length,url,s));}}if(rn){const z=rows.filter(r=>norm(r.round)===norm(rn));if(z.length){const w=z.filter(r=>won(r,player)).length;out.push(stat(player,"same_round_matches",z.length,z.length,url,s),stat(player,"same_round_win_pct",100*w/z.length,z.length,url,s));}}if(lv){const z=rows.filter(r=>norm(r.eventLevel)===norm(lv));if(z.length){const w=z.filter(r=>won(r,player)).length;out.push(stat(player,"same_level_matches",z.length,z.length,url,s),stat(player,"same_level_win_pct",100*w/z.length,z.length,url,s));}}const latestRank=[...rows].reverse().map(m=>won(m,player)?n(m.winnerRank):n(m.loserRank)).find((x):x is number=>x!==null);if(latestRank!==undefined)out.push(stat(player,"ranking",latestRank,1,url,s));const ranks=rows.map(m=>won(m,player)?n(m.winnerRank):n(m.loserRank)).filter((x):x is number=>x!==null);if(ranks.length){const peak=Math.min(...ranks);out.push(stat(player,"peak_ranking",peak,ranks.length,url,s),stat(player,"ranking_gap_to_peak",latestRank!==undefined?latestRank-peak:0,ranks.length,url,s));}return out;}
export function getTennisDataHistoricalMatchCount(){return load().length;}

import type { MetricFinding, Researcher } from "./audit-pipeline";
import { reconstruct, sanitizeEvidence, type SourcedStat } from "./reconstruction/engine";
import { resilientResearcher } from "./resilient-audit-research.server";
import { getStyleMatchupStats, getStyleProfileStats } from "./style-matchup.server";
import { getTennisDataHistoricalStats } from "./tennis-data-history.server";
import { getExtendedTennisDataStats } from "./tennis-data-extended.server";
import { certifyMetricFinding } from "./metric-certification";
function familyCode(code:string){const m=String(code).match(/(\d{1,3})$/);return m?m[1].padStart(3,"0"):String(code).padStart(3,"0");}function summarize(stats:SourcedStat[]){return stats.length?stats.map(s=>`${s.key}=${Number(s.value).toFixed(2)}`).join("; "):null;}function usable(v:string|null,t:string){return v!==null&&t!=="UNAVAILABLE"&&t!=="EXCLUDED";}function unresolved(m:MetricFinding|undefined){return !m||!usable(m.p1_value,m.p1_treatment)||!usable(m.p2_value,m.p2_treatment);}function dedupe(stats:SourcedStat[]){const m=new Map<string,SourcedStat>();for(const s of stats){const k=`${s.key}|${s.player.toLowerCase()}|${s.surface??""}|${s.window??""}`,old=m.get(k);if(!old||(old.origin==="RECONSTRUCTED"&&s.origin==="DIRECT"))m.set(k,s);}return[...m.values()];}function withDeterministicReconstruction(stats:SourcedStat[]){const direct=sanitizeEvidence(stats),outcome=reconstruct(direct);return dedupe([...direct,...outcome.derived]);}function localHistorical(p:string,c:string){try{return dedupe([...getTennisDataHistoricalStats(p,c),...getExtendedTennisDataStats(p,c)]);}catch{return[];}}function sourcesFor(stats:SourcedStat[]){return stats.flatMap(s=>s.sources??[]).filter((s,i,a)=>a.findIndex(x=>x.source_name===s.source_name&&x.url===s.url)===i);}function selected(stats:SourcedStat[],keys:string[]){const w=new Set(keys);return stats.filter(s=>w.has(s.key));}
const HISTORICAL_KEYS:Record<string,string[]>={"001":["surface_win_pct","surface_matches"],"005":["win_pct","surface_win_pct","set_win_pct","matches_last_28_days","days_since_last_match"],"014":["ranking","peak_ranking","ranking_gap_to_peak"],"020":["same_level_matches","same_level_win_pct"],"027":["first_set_win_to_match_conversion_pct","one_set_up_collapse_rate_pct","deciding_set_closing_pct"],"028":["matches_last_28_days","days_since_last_match","same_round_matches","same_round_win_pct"],"030":["same_tournament_matches","same_tournament_win_pct"],"037":["straight_set_win_pct","deciding_set_win_pct","win_pct","surface_win_pct","recent_win_opponent_rank_mean"],"068":["current_streak_signed","longest_win_streak_observed"],"077":["season_matches_before_lock","matches_last_28_days","days_since_last_match","longest_observed_rest_gap_days"]};
const CONSERVATIVE_PARTIAL_FAMILIES=new Set(["007","008","009","010","011","035","055","068","080"]),NO_RETRY_COMPOSITE_FAMILIES=new Set(["007","008","009","010","011"]);
const STRICT_FIELDS:Record<string,Set<string>>={"007":new Set(["direct_common_opponents","record","win_pct","common_opponent_matches","common_opponent_wins","common_opponent_losses","common_opponent_win_pct","surface_matched_common_opponents","tournament_level_matched_common_opponents","common_opponent_recency_weighted_win_pct","common_opponent_strength_weighted_win_pct","common_opponent_weighted_set_margin","common_opponent_second_degree_strength_pct"]),"008":new Set(["set1_win_pct","set2_win_pct","set3_deciding_set_win_pct","historical_deciding_set_win_pct","win_after_losing_set1_pct","win_after_winning_set1_pct","second_set_after_losing_set1_win_pct"]),"009":new Set(["win_after_losing_set1_pct","tiebreak_win_pct","tiebreaks_played"]),"010":new Set(["straight_set_match_win_pct","straight_set_match_win_pct_comparable"]),"011":new Set(["performance_variance","performance_floor_ceiling_set_margin_range","close_match_dependency_pct","deciding_tiebreak_win_reliance_pct"])};
const STRICT_SOURCES:Record<string,RegExp>={"007":/PredixSport/i,"008":/DataHub ATP/i,"009":/DataHub ATP/i,"010":/(DataHub ATP|PredixSport)/i,"011":/(DataHub ATP|PredixSport)/i},STRICT_FAMILY:Record<string,string>={"007":"PUBLIC_COMMON_OPPONENT_NETWORK","008":"HISTORICAL_DATAHUB_FAMILY_008","009":"HISTORICAL_DATAHUB_FAMILY_009","010":"PUBLIC_STRAIGHT_SET_HISTORY","011":"PUBLIC_VOLATILITY_FLOOR_HISTORY"},STRICT_MISSING:Record<string,string[]>={"007":["full game-score comparison where unavailable","tournament-level matching where row level is absent"],"008":["first-break frequency","break-back rate","Set 1 to Sets 2/3 hold/return improvement"],"009":["break-consolidation rate","serving-for-set conversion","serving-for-match conversion","combined break/set/match pressure-point performance","high-leverage clutch hold/break performance"],"010":["independent Monte Carlo straight-set probability for both players"],"011":["official-ranking-based upset resistance"]};
function strictSegments(v:string|null,a:Set<string>){if(!v)return null;const k=v.split(";").map(x=>x.trim()).filter(Boolean).filter(x=>{const i=x.indexOf("=");return i>0&&a.has(x.slice(0,i).trim());});return k.length?k.join("; "):null;}
export function enforceFiveMetricWiring(metric:{code:string},finding:MetricFinding):MetricFinding{const f=familyCode(metric.code),a=STRICT_FIELDS[f];if(!a)return finding;const p1=strictSegments(finding.p1_value,a),p2=strictSegments(finding.p2_value,a),sources=(finding.sources??[]).filter(s=>STRICT_SOURCES[f].test(s.source_name)),ok=sources.length>0,p1ok=!!p1&&ok,p2ok=!!p2&&ok,missing=[...new Set([...(finding.missing_inputs??[]),...STRICT_MISSING[f]])];return{...finding,p1_value:p1ok?p1:null,p2_value:p2ok?p2:null,p1_treatment:p1ok?"PARTIAL":"UNAVAILABLE",p2_treatment:p2ok?"PARTIAL":"UNAVAILABLE",evidence_family:(p1ok||p2ok)?STRICT_FAMILY[f]:null,sources,unavailable_reason:(p1ok||p2ok)?`PARTIAL: exact-field guard retained only master-definition inputs; unsupported components remain missing (${STRICT_MISSING[f].join(", ")}).`:`UNAVAILABLE: no approved exact-field value with matching provenance survived the post-fix wiring guard.`,missing_inputs:missing};}
export function completionSweepHistoricalFinding(input:{p1:string;p2:string;context:string},metric:{code:string}):MetricFinding|null{const f=familyCode(metric.code),keys=HISTORICAL_KEYS[f];if(!keys)return null;const a=withDeterministicReconstruction(localHistorical(input.p1,input.context)),b=withDeterministicReconstruction(localHistorical(input.p2,input.context));let p1=selected(a,keys),p2=selected(b,keys);if(f==="027"){const x=p1;p1=p2;p2=x;}const p1Value=summarize(p1),p2Value=summarize(p2);if(!p1Value&&!p2Value)return null;return{metric_code:metric.code,p1_value:p1Value,p2_value:p2Value,p1_treatment:p1Value?"PARTIAL":"UNAVAILABLE",p2_treatment:p2Value?"PARTIAL":"UNAVAILABLE",differential:null,evidence_family:`TENNIS_DATA_HISTORY_${f}`,reliability:70,sample:String(Math.max(...[...p1,...p2].map(s=>s.sample??0),0))||null,unavailable_reason:!p1Value||!p2Value?"One player side lacked the sourced historical inputs required for this metric family.":null,missing_inputs:!p1Value||!p2Value?["sourced historical inputs for unsupported player side"]:undefined,sources:sourcesFor([...p1,...p2])};}
function prefer(a:MetricFinding|undefined,b:MetricFinding|null){if(!b)return a;if(!a)return b;const p1=usable(a.p1_value,a.p1_treatment),p2=usable(a.p2_value,a.p2_treatment);return{...a,p1_value:p1?a.p1_value:b.p1_value,p1_treatment:p1?a.p1_treatment:b.p1_treatment,p2_value:p2?a.p2_value:b.p2_value,p2_treatment:p2?a.p2_treatment:b.p2_treatment,evidence_family:a.evidence_family??b.evidence_family,reliability:a.reliability??b.reliability,sample:a.sample??b.sample,unavailable_reason:(p1||p2||b.p1_value||b.p2_value)?null:(a.unavailable_reason??b.unavailable_reason),missing_inputs:(p1&&p2)?undefined:(a.missing_inputs??b.missing_inputs),sources:[...(a.sources??[]),...(b.sources??[])].filter((s,i,x)=>x.findIndex(z=>z.source_name===s.source_name&&z.url===s.url)===i)} as MetricFinding;}
function conservativePartial(metric:{code:string},finding:MetricFinding){const f=familyCode(metric.code);if(!CONSERVATIVE_PARTIAL_FAMILIES.has(f))return finding;const a=finding.p1_treatment==="DIRECT"||finding.p1_treatment==="RECONSTRUCTED",b=finding.p2_treatment==="DIRECT"||finding.p2_treatment==="RECONSTRUCTED";return{...finding,p1_treatment:a?"PARTIAL":finding.p1_treatment,p2_treatment:b?"PARTIAL":finding.p2_treatment,unavailable_reason:(a||b)?finding.unavailable_reason??"Composite family capped at PARTIAL unless every master-definition component is independently verified.":finding.unavailable_reason} as MetricFinding;}
export const completionSweepResearcher:Researcher={...resilientResearcher,async metrics(input){const base=await resilientResearcher.metrics(input),by=new Map(base.map(m=>[String(m.metric_code),m]));for(const m of input.metrics)by.set(String(m.code),prefer(by.get(String(m.code)),completionSweepHistoricalFinding(input,m))!);const retry=input.metrics.filter(m=>!NO_RETRY_COMPOSITE_FAMILIES.has(familyCode(m.code))&&unresolved(by.get(String(m.code))));for(const m of retry){try{const r=await resilientResearcher.metrics({...input,metrics:[m]}),c=r.find(x=>String(x.metric_code)===String(m.code));if(c)by.set(String(m.code),prefer(by.get(String(m.code)),c)!);}catch{}}return input.metrics.map(m=>certifyMetricFinding(enforceFiveMetricWiring(m,conservativePartial(m,by.get(String(m.code))??{metric_code:m.code,p1_value:null,p2_value:null,p1_treatment:"UNAVAILABLE",p2_treatment:"UNAVAILABLE",differential:null,evidence_family:null,reliability:null,sample:null,unavailable_reason:"All configured direct and approved reconstruction paths were exhausted without sufficient sourced inputs.",missing_inputs:["no supported sourced inputs after completion sweep"],sources:[]}))));},async extractStats(input){const base=await resilientResearcher.extractStats?.(input)??[],historical=localHistorical(input.player,input.context),reconstructed=withDeterministicReconstruction([...base,...historical]),style=[...getStyleProfileStats(input.player,input.context),...getStyleMatchupStats(input.player,(input.opponent??""),input.context)];return dedupe([...reconstructed,...style]);}};
